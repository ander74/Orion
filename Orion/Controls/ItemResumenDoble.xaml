<!-- =============================================== -->
<!--     Copyright 2017 - Orion 1.0 - A. Herrero     -->
<!--                                                 -->
<!--  Vea el archivo Licencia.txt para mas detalles  -->
<!-- =============================================== -->
<UserControl x:Class="Orion.Controls.ItemResumenDoble"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             x:Name="Item">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Column="0" Margin="5,0" HorizontalAlignment="Left" Text="{Binding ElementName=Item, Path=Descripcion}"/>
        <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <TextBlock Margin="5,0,0,0" Text="{Binding ElementName=Item, Path=Valor1}"/>
            <TextBlock Margin="0" Text="{Binding ElementName=Item, Path=Separador}"/>
            <TextBlock Margin="0,0,5,0" Text="{Binding ElementName=Item, Path=Valor2}"/>
        </StackPanel>

    </Grid>
</UserControl>
